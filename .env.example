# Environment Variables Example
# Copy this file to .env.local and update with your values

# API Base URL (only needed for production with separate backend)
# In development, vite.config.ts proxies /api to http://localhost:8000
# VITE_API_URL=https://your-backend-domain.com/api

# Leave empty or commented out for development (uses localhost:8000)
# Uncomment and set for production deployments with separate backends
